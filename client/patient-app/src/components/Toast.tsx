/* eslint-disable react-refresh/only-export-components --
   A re-export barrel. The rule states outright that it cannot verify an
   `export *`; there is no component defined here to verify. */
/**
 * Shared Toast Notification System (Re-exported from shared library)
 */
export * from '@medichain/shared';
